# ============================================================================
# CRS-FULL — Dependencias de producao
# ============================================================================
# Apenas o que o servico realmente usa em runtime.
# Bibliotecas de audio (librosa, soundfile) e SDKs nao utilizados foram
# removidos: o motor acustico nao roda neste repositorio, e sim no crs-core.
# Manter peso morto aqui so aumenta o tempo de build no Render.
# ============================================================================

# --- Servidor web -----------------------------------------------------------
Flask>=3.0,<4.0
gunicorn>=21.2,<24.0

# --- Extensoes Flask --------------------------------------------------------
Flask-Cors>=4.0,<7.0
Flask-SQLAlchemy>=3.1,<4.0

# --- Autenticacao -----------------------------------------------------------
PyJWT>=2.8,<3.0

# --- Banco de dados ---------------------------------------------------------
# Driver oficial do Postgres usado pelo SQLAlchemy.
# A URL vem em formato postgresql+psycopg2:// (o main.py normaliza).
psycopg2-binary>=2.9,<3.0

# --- Utilidades -------------------------------------------------------------
python-dotenv>=1.0,<2.0